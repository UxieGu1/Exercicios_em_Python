# Calcular a área de um triângulo a partir de sua base e altura

base = float(input('Insira a base do triângulo: '))
altura = float(input('Insira a altura do triângulo: '))
print(f'A área do triângulo e: {base * altura / 2}')